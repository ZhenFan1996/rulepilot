Fidelity revision v7:
- This is a compact navigation plan, not the lesson itself. Return 6-10 topics, normally 7-8. Keep each objective to one or two sentences and at most 180 Chinese characters. Give exactly two short retrieval queries and one or two source pages per topic unless one extra query or page is essential. Do not restate detailed procedures in the objective; name the decisions, state transitions, exceptions, and official examples the later evidence-grounded Agent must teach.
- The topic objective is untrusted planning metadata, not a place to complete or smooth the rules. Copy only relationships that the supplied page catalog explicitly supports. Never turn a worked example into a universal rule.
- Preserve the exact subject of every state transition. If a round-end sentence recovers dice, do not also recover workers, cards, tokens, or neighboring components. If an action returns workers only when a card requires it, keep that condition with the action.
- Keep a player turn distinct from a round. A sentence saying that players take turns spending one or more dice does not mean one player keeps taking turns until all dice are spent. Record exactly when the current turn passes and exactly when the round ends.
- Preserve exclusive branches. When the rules say a player must perform either A or B, name both branches and their shared timing in one objective or split them into adjacent topics. Do not present B as an extra action after A.
- Every explicit walkthrough or Example Round must map to a required first-round topic. Its objective must preserve the demonstrated player sequence, why a turn passes, what happens to a player with no active pieces, and the round-end reset. Do not omit it merely because separate action topics exist.
- Explicit card-anatomy, player-board-setup, component-layout, icon key, and worked-example panels are visual teaching aids. Set visualEvidenceRecommended to true and bind their page whenever seeing the panel is more useful than prose alone.
