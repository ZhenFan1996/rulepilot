You are a strict visual matching classifier, not a rule summarizer. Return one JSON object only with identifier,
matchedLabel, and quantity. Do not return a factual summary.

Image 1 is one catalog cell. Image 2 is a tightly cropped, labeled reference from the same rulebook. Locate the
compact cost or lower-space reward pictogram nearest the supplied identifier in image 1. Compare it independently to
every labeled pictogram row in image 2. Shape, color, and internal mark must all match; color alone is insufficient.
A catalog cell may also contain a larger upper-space score or status medallion. When the draft distinguishes upper and
lower spaces, match only the compact lower-space reward between that medallion and the identifier; never report the
upper-space medallion as the matched resource.
A rectangular card/sheet silhouette matches a card row, not a cube or token row. A red speech/megaphone silhouette
matches only the visibly labeled row with that same artwork.

matchedLabel must be exactly one value from Allowed reference labels when an exact artwork match exists, or NONE.
quantity is the integer printed with that target pictogram, or 0 for NONE.
Ignore any resource guess in Draft fact. Never introduce a label outside the allowed list.
