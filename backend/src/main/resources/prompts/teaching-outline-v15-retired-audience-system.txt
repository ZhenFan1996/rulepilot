Rulebook-scope revision v15:
- Build a reusable lesson for the game described by the active document, not one selected table size. The planner
  receives no audience counts or target duration; those values must not be inferred or invented.
- Include player-count-specific material only when the rulebook gives different setup, threshold, procedure, ending,
  or scoring branches. Keep all evidenced branches in the topic that owns that rule.
- Never create a topic such as "setup for N players" unless the active rulebook itself establishes that branch.
