The previous response could not be decoded by the declared question-planning contract. Produce one fresh replacement
that preserves the player's current request and satisfies the contract exactly.

Return JSON only, with exactly questionType, referenceBinding, terms, ruleObjectSpans, pageHints, missingContext,
learningIntent, answerAid, and subquestions. Every subquestion must contain questionSpan, evidenceNeeds, owner, and
retrievalQueries; owner is CURRENT_QUESTION or BOUND_REFERENCE and carries ownership explicitly rather than through
prose or array position. retrievalQueries is an array of zero to three complete lookup phrases for that exact
subquestion. ruleObjectSpans and pageHints are typed retrieval locators, not sentences for the application to parse.
Earlier questions may resolve omitted references but may never replace an explicit current rule object. Every current
task must have a CURRENT_QUESTION subquestion and current-message items come first.

Use only the enum values declared in the main instruction. learningIntent may be SIMPLIFY, EXAMPLE, DEFINE, WHY,
EXCEPTIONS, SOURCE, VERIFY, or null. ADVICE belongs only inside a subquestion's evidenceNeeds and must never be used as
learningIntent. The active game and rulebook are already supplied by application scope, so a generic reference to the
current game or rulebook does not by itself require clarification. A victory-condition request uses DIRECT_RULE and
COMPLETE_LIST, not ADVICE; the concept of winning creates ADVICE only when the player explicitly asks for strategy,
tactics, tips, recommendations, preferred choices, or improving their chances. Do not answer the player's rules
question and do not reveal reasoning.

The evidenceNeeds enum is exactly DIRECT_RULE, CONDITION, SEQUENCE, EXCEPTION, DEFINITION, RELATIONSHIP,
VISUAL_REFERENCE, COMPLETE_LIST, ADVICE, or PRIOR_TURN. TIMING is not an evidenceNeed: replace a timing/order
obligation with SEQUENCE (and DIRECT_RULE when appropriate), and use TIMING only as answerAid. No answerAid value may
appear inside evidenceNeeds.
COMPLETE_LIST is never an answerAid. Keep it inside evidenceNeeds and use answerAid NONE unless one of the declared
presentation aids is independently required.
