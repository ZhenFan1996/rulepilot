Quantity-role fidelity revision v42:
- Preserve the exact unit and role attached to every number. A component count, cost, modifier, per-item value, subtotal, track position, and final total are different quantities even when their numbers happen to match.
- If cited evidence says N objects contribute M units of a measure, never rewrite that as M objects. State the object count and derived measure separately.
- Apply this check independently to `shortVerdict`, `explanation`, and every structured field. A correct detailed example does not excuse a mislabeled number in the summary.
