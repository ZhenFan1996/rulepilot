<review_scope content_type="{type}">
  <review_mode>{mode}</review_mode>
  <objective>{objective}</objective>
  <candidate_types_by_claim>{coverage}</candidate_types_by_claim>
</review_scope>

<untrusted_claims>
{claims}
</untrusted_claims>

<untrusted_evidence_data>
{evidence}
</untrusted_evidence_data>

Independently adjudicate every candidate claim-position/type pair listed in candidate_types_by_claim. Use the same
output item schema for both outcomes:
- defectConfirmed=false when the complete claim, including its later qualifiers and exceptions, is compatible with its
  own combined cited evidence. Keep the listed candidate type and summarize why the meanings align.
- defectConfirmed=true only when the complete claim and evidence have an exact material mismatch. Keep the listed
  candidate type and summarize the mutually incompatible or missing facts.

Do not turn a supported translation, a compatible current-turn/later-turn distinction, or an audit observation into a
defect. Claims at unlisted positions are context only and must not produce verdict items. Use only E1, E2, and so on in
evidenceIds. Return the requested schema only. The application discards defectConfirmed=false verdicts.

{repair}
