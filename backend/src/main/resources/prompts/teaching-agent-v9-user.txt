<task>
  <section>{section}</section>
  <section_objective>{objective}</section_objective>
  <coverage_checklist>{coverage}</coverage_checklist>
  <required_rule_intents>{requiredRules}</required_rule_intents>
  <audience player_count="{players}" beginner_count="{beginners}" />
  <lesson_pacing total_minutes="{totalDuration}" section_seconds="{sectionDuration}" max_steps="{maxSteps}" />
  <visual_evidence available="{visualEvidenceAvailable}" required_visual_step="{visualEvidenceAvailable}" attached_rulebook_pages="{visualPages}" />
  <output_language>Simplified Chinese with readable official game terms</output_language>
</task>

<untrusted_prior_section_continuity_data>
{continuity}
</untrusted_prior_section_continuity_data>

<untrusted_current_section_evidence_data>
{evidence}
</untrusted_current_section_evidence_data>

Treat every required_rule_intents item as a material rule to teach when the supplied evidence supports it. Before
returning, map each intent to at least one player-facing step or confirm that the evidence does not contain that rule.
Never copy an intent as a vague heading and never invent a detail merely because the intent asks for it.

When visual_evidence.available is true, the output is invalid unless at least one step has kind VISUAL and a non-null
visualFocus on an attached page. Keep that step practical: tell the player what region to locate and what evidenced
relationship to notice. This requirement also applies to every revision.

Before returning the schema, silently check that a beginner can answer all three questions from the result:
1. What am I trying to understand in this topic?
2. What do I do or observe at the table?
3. How do I know I can move on?
If evidence does not support one answer, omit that claim rather than inventing it.

{repair}
