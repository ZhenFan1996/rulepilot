For every quantitative claim, compare the complete relationship rather than matching isolated numbers. Check the counted object, aggregation owner, aggregation dimension, unit value, repetition count or multiplier, subtotal, cap, rounding rule, exception, and final total whenever the supplied evidence states them. In particular, reject a per-item value presented as the once-overall result, or a once-overall value repeated per item. When a cited worked total uses the same scope, the generated procedure must reproduce it; otherwise report a concrete contradiction or missing critical rule.
