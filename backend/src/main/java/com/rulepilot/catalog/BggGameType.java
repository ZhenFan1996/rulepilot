package com.rulepilot.catalog;

/** Public BGG ranking domains shared by catalog consumers without exposing catalog internals. */
public enum BggGameType {
    ALL,
    ABSTRACT,
    CUSTOMIZABLE,
    CHILDREN,
    FAMILY,
    PARTY,
    STRATEGY,
    THEMATIC,
    WAR,
    EXPANSION
}
